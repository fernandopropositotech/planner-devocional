/** @type {import('next').NextConfig} */
const nextConfig = {
  eslint: {
    // ⚠️ Permite que o build continue mesmo com erros do ESLint
    ignoreDuringBuilds: true,
  },
};

module.exports = nextConfig;










